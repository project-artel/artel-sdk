using System.Runtime.CompilerServices;

// 이 어셈블리는 전부 internal 이다. 안에 든 것이 게임의 IL 을 읽는 규칙들이고, 그것을 확인하려면 그 규칙을
// 부를 수 있어야 한다. 리포트를 통째로 만들어 놓고 문자열을 뒤지는 것은 무엇이 틀렸는지 말해 주지 않는다.
[assembly: InternalsVisibleTo("Artel.Affordances.CodeGen.Tests")]
